generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model workflow {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  inputText String
  results   Json
}